TAGCheck Campo APK - corrigido

Correções aplicadas:
- Câmera corrigida usando FileProvider.
- Foto tirada pela câmera agora retorna para o campo do formulário.
- Dependência androidx.core adicionada no app/build.gradle.
- file_paths.xml mantido em app/src/main/res/xml/.

Para gerar o APK:
1. Abra este projeto no Android Studio.
2. Clique em Sync Now, se aparecer.
3. Build > Clean Project.
4. Build > Rebuild Project.
5. Build > Build Bundle(s) / APK(s) > Build APK(s).

Se instalar por cima de APK antigo, desinstale o antigo antes.
